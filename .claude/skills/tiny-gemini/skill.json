{
	"name": "tiny-gemini",
	"version": "1.0.1",
	"description": "Teaches AI agents to use the tiny-gemini NPX CLI for the Google Gemini API. Covers text, image, TTS, search, research, and raw API commands.",
	"keywords": [
		"ai",
		"api",
		"gemini",
		"google",
		"image-generation",
		"tts",
		"cli"
	],
	"repository": "https://github.com/nicolasdao/tiny-gemini",
	"systemDependencies": {
		"node": {
			"description": "Node.js runtime",
			"version": ">=18.0.0",
			"check": "node --version",
			"install": {
				"darwin": "brew install node",
				"linux": "curl -fsSL https://deb.nodesource.com/setup_lts.x | sudo -E bash - && sudo apt-get install -y nodejs"
			}
		},
		"npx": {
			"description": "NPX package runner (bundled with Node.js)",
			"version": ">=8.0.0",
			"check": "npx --version",
			"install": {
				"darwin": "Included with Node.js installation",
				"linux": "Included with Node.js installation"
			}
		}
	}
}
